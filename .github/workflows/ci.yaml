---
name: CI

on:
  pull_request:
    branches:
      - main
  
permissions:
  contents: read
  pull-requests: write

jobs:
  terraform-plan:
    name: Terraform Plan
    uses: luisdibdin/aws-terraform/.github/workflows/terraform-plan.yaml@lambda-layer
    secrets:
      inherit